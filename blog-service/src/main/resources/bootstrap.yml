#server:
#  port: 9001

spring:
  application:
    name: blog
  cloud:
    config:
      profile: service
      uri: http://localhost:9002
#      discovery:
#        enabled: true
#        service-id: blog-config